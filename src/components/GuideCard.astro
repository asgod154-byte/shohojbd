---
import PostCard from "./PostCard.astro";

interface Props {
  title: string;
  description: string;
  href: string;
}

const { title, description, href } = Astro.props;
---
<PostCard post={{ id: href, data: { title, description, pubDate: new Date(), category: "", tags: [] }, collection: "", body: "" }} variant="horizontal" />
